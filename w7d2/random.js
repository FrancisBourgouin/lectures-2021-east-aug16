const bob = [1, 2, 3, 4];
const robert = [...bob];



$.ajax("get", "/urls", ["..."])

$.ajax({method:"POST",url:"/urls",data:["..."]})

fs.readFile("/path", {options}, () => )

const bob = {
  age:1
}